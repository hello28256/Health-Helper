FROM node:20-alpine

WORKDIR /app

# 安装 OpenSSL 用于 Prisma 在 Alpine 上的连接
RUN apk add --no-cache openssl

# 单独复制依赖文件以利用缓存
COPY package*.json ./
COPY prisma ./prisma

RUN npm install

# 生成 Prisma client
RUN npx prisma generate || true

COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]
